Review the current `git diff` (staged and unstaged). Group the changes into logical commits by feature, fix, or area of the codebase. For each group:

1. Stage only the relevant files with `git add`
2. Commit with a clear, conventional commit message (e.g. `feat:`, `fix:`, `chore:`, `docs:`, `refactor:`)

Before committing, show me the planned groups and messages for approval.
